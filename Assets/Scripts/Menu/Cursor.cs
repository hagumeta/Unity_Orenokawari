using System.Collections;
using System.Collections.Generic;
using UnityEngine;


namespace Menu
{
    public abstract class CursorBase : MonoBehaviour
    {
        /*
        protected int _cursor { get; set; }
        abstract public int cursor { get; set;}
        abstract protected void Enter();
        
        virtual protected void Update()
        {
            if (Input.GetButtonDown("Vertical"))
            {
                int verticalAxis = (int)Input.GetAxisRaw("Vertical");
                if (verticalAxis != 0)
                {
                    this.cursor -= verticalAxis;
                }
            }

            if (Input.GetButtonDown("Submit"))
            {
                this.MenuIndexes[this.cursor].SubmittedEvent.Invoke();
            }
        }
    }

    public abstract class C
    

    */
    }
}