using UnityEngine;
using UnityEngine.Events;
/*
public class GameEventListener : MonoBehaviour
{
    [SerializeField] private GameEvent<GameEventListener> gameEvent;
    protected virtual GameEvent<GameEventListener> GameEvent
           => this.gameEvent;
    private void OnEnable()
    {
        this.GameEvent.RegisterListener(this);
    }

    private void OnDisable()
    {
        this.GameEvent.UnregisterListener(this);
    }
    virtual public void OnEventRaised()
    {
    }
}

*/